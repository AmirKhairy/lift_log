enum Gender { male, female }

enum MuscleGroup {
  push,
  pull,
  legs,
  chest,
  back,
  shoulders,
  arms,
  glutes,
  core,
  cardio,
}

enum TutorialVideoPlaybackType { youtube, directVideo, webPage, invalid }
